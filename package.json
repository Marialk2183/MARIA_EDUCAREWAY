{
  "name": "educareway-platform",
  "version": "1.0.0",
  "description": "Educational platform for NMIMS with React, Node.js, Express, PostgreSQL, and Firebase",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["education", "react", "node", "express", "postgresql", "firebase"],
  "author": "Maria & Umed",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

